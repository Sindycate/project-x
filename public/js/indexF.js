'use strict';

$(document).ready(function() {

  $('.owl-carousel').owlCarousel({
    loop:true,
    // margin:10,
    nav:false,
    dots: false,
    // autoplay: true,
    autoplayTimeout: 3500,
    // dotsEach: true,
    autoplayHoverPause: true,

    // autoWidth:true,
    items: 1
  });

});